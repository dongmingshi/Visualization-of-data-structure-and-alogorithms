package Demo.InsertionSort;

import Demo.SortData;

public class InsertionSortData extends SortData {

    int orderedIndex = -1;
    int curIndex = -1;

    InsertionSortData(int N, int randomBound) {
        super(N, randomBound);
    }
}
